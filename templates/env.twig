# Database
DB_NAME="{{ name }}"
DB_USER="{{ user }}"
DB_PASSWORD="{{ password }}"
DB_HOST="localhost"
DB_CHARSET="utf8"
DB_COLLATE=""

{{ salts|raw }}

WP_HOME="{{ url }}"
WP_SITEURL="${WP_HOME}/wordpress"
WP_CONTENT_URL="${WP_HOME}/app"

# WP debug
WP_DEBUG="{{ debug }}"

# Wordpress
AUTOMATIC_UPDATER_DISABLED=""
DISABLE_WP_CRON=""
DISALLOW_FILE_EDIT=""
DISALLOW_FILE_MODS=""
WP_POST_REVISIONS=""
